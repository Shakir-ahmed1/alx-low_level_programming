#include "main.h"
/**
 * add - it adds two integers
 * @num1: first operand
 * @num2: second operand
 * Return: returns the sum of the tow arguments
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
