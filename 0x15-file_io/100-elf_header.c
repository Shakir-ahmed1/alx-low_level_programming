#include "main.h"
/**
 * main - the main function
 * @argc: the number of arguments
 * @argv: the array of strings that holds the paramaters
 * Return: always zero
 */
int main(int argc, char **argv)
{
	(void) argc;
	(void) argv;

	return (0);
}
