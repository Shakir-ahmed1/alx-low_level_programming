#include "main.h"
/**
 * _abs - it returns the absolute value of the given number
 * @n: the number to be changed
 * Return: always + for all intigers and 0 if input is 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
